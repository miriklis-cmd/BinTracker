# BinTracker v0.4.0-alpha.5 — Import Wizard Layout Fixes

- Increased vertical clearance in the wizard progress indicator so step subtitles are not clipped.
- Kept circular steps and the connecting progress line.
- Moved `View all worksheets` into a dedicated grid footer so its full caption remains visible.
- Replaced the long single-line workbook result with a compact multi-field analysis summary.
- Long workbook warnings now render in a scrollable multiline warning box.
- Reduced preview-grid heights and vertical padding so the customer candidate section is visible sooner.
- Added unique customer count separately from total customer occurrences.
- No database import/write is enabled yet.
